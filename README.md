# Basic-MIPS-Interpreter
Built a basic MIPS interpreter written in C++ that allows the user to run it in two modes: immediate and batch.  Immediate mode is utilized to perform arithmetic, logical, shift, comparison, control, and load/store instructions.  Batch mode is utilized to test and run full programs. It uses MIPS instruction format in its design.
